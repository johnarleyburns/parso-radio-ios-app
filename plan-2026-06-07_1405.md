# Plan — 2026-06-07

## 1. Add 5 default iCloud podcasts

**File:** `ParsoRadio/Core/Models/Channel.swift`
- Add 5 new `Channel` entries after line 306 (after CBC As It Happens), before `// MARK: Curated`
- RSS feeds: Joe Rogan (feeds.simplecast.com/dZbMGr98), The Daily (feeds.simplecast.com/54nAGcIl), This American Life (thisamericanlife.org/podcast/rss.xml), TED Radio Hour (npr.org/rss/podcast.php?id=510298), NPR Politics (feeds.npr.org/510310/podcast.xml)
- Each: `category: "Podcasts"`, `contentType: .spokenWord`, `preferredSource: "podcast"`, `tags: [id]`

**File:** `ParsoRadio/UITests/PodcastUITests.swift:100` — update expected channels 7 → 12

## 2. Fix podcast count mismatch

**File:** `ParsoRadio/Views/MainMenuView.swift`
- Add `@ObservedObject private var podcastStore = PodcastSubscriptionStore.shared`
- `channels(in:)`: for "Podcasts" category, merge user subs from podcastStore

## 3. Apply jerky fix to curation search results

**File:** `ParsoRadio/Views/CuratorSearchAddView.swift`
- Line 96-97: Replace `.animation(...)` with `.animation(.none, value:)` to prevent List row-recycle animation jank
- Lines 208-213: Batch async verdict lookups concurrently instead of sequential for-loop

## 4. Default sort "downloads desc"

**File:** `ParsoRadio/Core/Services/API/InternetArchiveService.swift`
- Line 107: `sort[]=random` → `sort[]=downloads desc` (Load More Candidates)
- Line ~481: Add `sort[]=downloads desc` to `searchGroups()` (curator search + general search)

## 5. Fix autoplay-after-failure loop

**File:** `ParsoRadio/Views/CuratedChannelsListView.swift:604`
- `$0.id != id` → `!failedTrackIds.contains($0.id)` — exclude ALL previously failed tracks

## 6. Fix export naming (add date)

**File:** `ParsoRadio/Core/Services/CustomChannelsStore.swift:450`
- `"lorewave-\(slug).json"` → `"lorewave-\(slug)-\(dateStr).json"`

**File:** `ParsoRadio/Views/ChannelInfoView.swift:38`
- Same date addition in FileRepresentation closure
